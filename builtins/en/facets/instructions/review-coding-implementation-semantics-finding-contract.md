{{include:instructions/review-coding}}

## Finding Contract Additional Procedure

- Apply every section of Implementation Semantics Knowledge in the same review as the general coding perspective
- Trace changed state, identifiers, names, metadata, configuration, and output contracts from their producer through existing namespaces, persistence, display, lookup, and reuse
- For each target, record the concrete collision or semantic mismatch condition and the actual verification result in the evidence table
- Do not approve semantics based on passing tests alone; verify downstream interpretation in code
