計画上の実装範囲とタスク開始時点からの累積差分を照合し、レビュー範囲を1つに分類してください。
実装やファイル編集は行わず、判定結果を structured output だけに出力してください。

**計画上の実装範囲:** `{structured:classify_implementation_scope.work_type}`

**やること:**
1. 計画上の実装範囲を必ずレビュー対象に含める
2. タスク開始時点からの累積差分を確認し、実際に変更された領域を追加する
3. `レビュー範囲 = 計画上の実装範囲 ∪ 実際に変更された領域`として分類する
4. 計画された変更が差分に存在しなくても、実装漏れの可能性があるため範囲を縮小しない
5. 計画範囲が`backend-cqrs` / `dual-cqrs`の場合、または実際の変更がCQRS+ESに及ぶ場合はCQRS分類を維持・拡張し、どちらにも該当しない場合だけ非CQRS分類を使う
6. `work_type`に分類値、`rationale`に計画範囲と差分の根拠を簡潔に出力する
