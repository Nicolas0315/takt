name: takt-default
description: TAKT固有ファセットと動的実装 companion を共通開発フローへ注入する開発ワークフロー。
max_steps: 51
initial_step: develop
steps:
  - name: develop
    kind: workflow_call
    call: development-core
    args:
      implementation_pool: takt-coding-facets
      plan_policy: [design-planning]
      plan_knowledge: [takt, architecture, task-decomposition]
      implementation_workflow: development-implement-dynamic
      remediation_workflow: development-remediation-dynamic
      implementation_companions:
        fixed: [ai-antipattern-review-companion]
        pool: []
        moderator: ai-antipattern-review-moderator
      reviewer_suite: takt-default-development-review-adapter
      review_policy_additions: [takt-testing]
      testing_policy: [coding, testing, takt-testing]
      testing_knowledge: [takt, architecture, unit-testing, e2e-testing]
      development_policy: [coding, testing, takt-testing, ai-antipattern]
      development_knowledge: [takt, architecture, implementation-semantics]
      implementation_instruction: implement
      review_policy: [coding, testing, takt-testing, review]
      review_knowledge: [takt, architecture, unit-testing, e2e-testing]
    rules:
      - condition: COMPLETE
        next: COMPLETE
      - condition: ABORT
        next: ABORT
